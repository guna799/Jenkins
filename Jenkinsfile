pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                echo ' Testing webhook triggers'
                echo ' adding triggers in Git  adfe'
            }
        }
    }
}
